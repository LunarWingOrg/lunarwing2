[package]
name = "weechat-relay-channel"
version = "1.1.9"
edition = "2021"
description = "WeeChat Relay WASM channel for LunarWing"
license = "AGPL-3.0-or-later"

[lib]
crate-type = ["cdylib"]

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

# Must match LunarWing's channel interface version
# Check your LunarWing repo's channels-src/weechat/Cargo.toml for the correct version
wit-bindgen = "0.36"

[profile.release]
opt-level = "s"
lto = true
strip = true

[workspace]
