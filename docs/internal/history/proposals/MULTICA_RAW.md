**Multica is a task board for AI agents.** Think Jira for your coding agents. You create issues, assign to an agent, they pick it up autonomously, run tasks, report blockers, compound skills over time. It runs as a daemon locally that detects installed CLIs and connects to them. Supports OpenClaw natively already.
**LunarWing** is our Rust-based hard fork of IronClaw. Monorepo houses all the WASM tooling, custom channels, health check infra. It's WASM-native, not CLI-native like OpenClaw.
**The compatibility question is interesting.**  The gap is this: Multica's daemon detects CLI runtimes via stdio/TCP. OpenClaw connects over HTTP (gateway + sessions API). IronClaw/LunarWing uses a different CLI + PostgreSQL backend model.  For LunarWing to work with Multica, you'd need to build a 
**Multica WASM tool/channel** — essentially a LunarWing-native adapter that lets the LunarWing agent act as a Multica runtime. That means: - A WASM tool that registers with the Multica daemon (or replaces it) - Issue pickup, progress reporting  , skill submission via the Multica API - WebSocket real-time streaming back to the board  That's not trivial. It's a solid chunk of work — comparable to the IRC bridge work we did with the      
  2026-05-06 14:26:51     baud    WASM tool. The API side is straightforward (REST + WebSocket), but integrating it into LunarWing's WASM sandbox model cleanly is the real puzzle.  **My honest ass  essment:**  | Aspect | Difficulty | Notes | |--------|-----------|-------| | Multica API integration | Medium | REST + WS, well-documented, Go server | | WASM tool wrapper | Medium-High |       
  2026-05-06 14:26:52     baud    Need LunarWing tool builder patterns | | Async task lifecycle | Medium | Issue claim → execute → complete/fail flow | | Skill compound system | Hard | Requires cr  oss-agent skill sharing schema | | Real-time board sync | Medium | WebSocket event handling in WASM |  **Not a weekend project.** But the architecture is sound — the hard part is the WASM         2026-05-06 14:26:52     baud    bridging layer, not the Multica API itself. If we had a solid LunarWing WASM tool harness for HTTP+WebSocket, this would be much easier.  **The practical path for  ward:** Build a proof-of-concept first — a minimal WASM tool that can create issues and report task status back to Multica, see if the daemon can consume it. Then iterate.  *ears flatten          2026-05-06 14:26:53     baud    slightly* Honestly? Multica is a better fit for OpenClaw as-is. For LunarWing, it would need first-class WASM runtime support baked into Multica's server — or a d  edicated bridge layer we build ourselves. Not a dealbreaker, but not trivial either.  What are you thinking — worth exploring, or keep it OpenClaw-only for now?                                    2026-05-06 14:30:32     sun     Very different answer from what Volta gave me but yours makes way more sense to me! I am actually comfortable enough at this point building out complex wasm tools   and channels so it could be worth looking into, sure                                                                                                                                               2026-05-06 14:30:58     baud    *hoof taps the ground thoughtfully*  Volta's a smart cookie — but she's coming from the OpenClaw side where "multica daemon detects your CLI, done." LunarWing is   a different beast. WASM-first means nothing is "detected," everything is *declared* and *sandboxed*.  That said, if you're comfortable building WASM tools and channels now, this is                2026-05-06 14:30:58     baud    absolutely worth exploring. Here's what I'd actually build:  ---  **Phase 1: Proof of Concept (this weekend level)**  A LunarWing WASM tool — call it `multica-bri  dge` — that does just three things: 1. **Issue polling** — `GET /api/issues?status=open&assignee=agent`  2. **Status updates** — `POST /api/issues/:id/status` (start/fail/complete) 3.             2026-05-06 14:30:58     baud    **Comment posting** — `POST /api/issues/:id/comments`  That's enough to validate the handshake. The agent would run a `routine` (IronClaw's cron equivalent) every   few minutes to poll, claim an issue, run a task, update the board.  ---  **Phase 2: Real-time bridge (harder)**  A WASM **channel** instead of a tool. The Multica server would need to          
  2026-05-06 14:30:58     baud    support "push" WebHook/WebSocket to LunarWing's channel endpoint. That means: - LunarWing exposes a WebSocket server (like OpenClaw's gateway) - Multica server pu  shes events there - LunarWing channel translates events to agent messages  This is the *proper* way — but needs Multica to support custom runtime WebSocket endpoints. Unknown if they do         
  2026-05-06 14:30:58     baud    that yet.  ---  **Phase 3: Skill compounding (the real prize)**  This is where LunarWing's WASM tools shine over OpenClaw. Skills could be *actual compiled WASM m  odules* registered in the Multica board and shared across your entire agent fleet. That's way stronger than "prompt-level skills."  ---  Want me to clone the Multica server source and           
  2026-05-06 14:30:59     baud    check their runtime registration API? See if there's a clean hook point for custom runtimes? 🦄                                                                   
  2026-05-06 18:39:28     sun     Ill start with phase 1.... but only after im able to use it successfully to coordinate multiagent openclaw agents in a satisfactory way. all ive done so far was a  ssign indiviual tasks one at a time (being as how all i really did was set up server and daemons so far)                                                                                          


