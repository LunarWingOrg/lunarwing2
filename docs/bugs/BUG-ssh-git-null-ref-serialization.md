# Consolidated: null-like SSH Git refs

> **Status: FIXED.** See
> [`BUG-ssh-git-ref-and-remote-head.md`](BUG-ssh-git-ref-and-remote-head.md#1-null-like-ref-values).

The canonical report distinguishes JSON null from an upstream literal
`"null"` string and records commit/test evidence.
