# Additions

> **Current status (2026-07-20, rev `50c8f99`): HISTORICAL RECORD.**
> `LLM_MODEL`, `GATEWAY_HOST`, and `XMPP_ALLOW_FROM` are configurable in the
> shipped `add-tenant` flow; `lunarwing_mt_onboard` is the supported guided path.

## to make flags for to make configurable

* LLM_MODEL
* GATEWAY_HOST
* XMPP_ALLOW_FROM
