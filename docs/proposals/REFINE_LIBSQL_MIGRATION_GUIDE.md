# Refine libSQL Migration Guide

> **Current status (2026-07-20, rev `50c8f99`): OPEN.** A libSQL migration guide
> exists under `docs/guides/`, but the proposed runtime-agnostic refinement and
> script cleanup have not been completed.

> **TODO.** Refine the libSQL migration guide (`docs/guides/`) and make the
> migration scripts runtime-agnostic so anyone can run them easily.
