x-worker-base: &worker-base
  build:
    context: .
    dockerfile: Dockerfile
  image: lunarwing-worker-opencode:latest
  working_dir: /workspace
  group_add:
    - "${HOST_GID:-1000}"
  volumes:
    - ${OPENCODE_CONFIG_FILE:-./config/opencode.json}:/app/config/opencode.json:ro,z
    - opencode-data:/home/opencode/.local/share/opencode:z
    - opencode-config-home:/home/opencode/.config/opencode:z
    - ./workspace:/workspace:z
    - ./logs:/var/log/lunarwing:z
    - ./results:/app/results:z
    - ${SSH_DIR:-./.ssh-placeholder}:/home/opencode/.host-ssh:ro
    - ${GITCONFIG_FILE:-./.gitconfig-placeholder}:/home/opencode/.host-gitconfig:ro
  env_file:
    - path: ${EXTRA_ENV_FILE:-.env.secrets}
      required: false
  environment: &worker-env
    TENSORZERO_API_KEY: ${TENSORZERO_API_KEY:-dummy}
    OPENAI_API_KEY: ${OPENAI_API_KEY:-}
    OPENCODE_SERVER_PASSWORD: ${OPENCODE_SERVER_PASSWORD:-}
    OPENCODE_SERVER_USERNAME: ${OPENCODE_SERVER_USERNAME:-opencode}
    AGENT_AUTH_TOKEN: ${AGENT_AUTH_TOKEN:-}
    LUNARWING_WORKER_ID: ${LUNARWING_WORKER_ID:-worker-opencode-01}
    WORKSPACE_ROOT: /workspace
    OPENCODE_ROOT: /app/opencode/packages/opencode
    FILE_UMASK: ${FILE_UMASK:-0002}
    GITHUB_TOKEN: ${GITHUB_TOKEN:-}
    GH_TOKEN: ${GH_TOKEN:-}
    GIT_AUTHOR_NAME: ${GIT_AUTHOR_NAME:-}
    GIT_AUTHOR_EMAIL: ${GIT_AUTHOR_EMAIL:-}
    GIT_COMMITTER_NAME: ${GIT_COMMITTER_NAME:-}
    GIT_COMMITTER_EMAIL: ${GIT_COMMITTER_EMAIL:-}
    PASEO_URL: ${PASEO_URL:-}
    PASEO_TOKEN: ${PASEO_TOKEN:-}

services:
  lunarwing-worker:
    <<: *worker-base
    command: ["--mode", "websocket"]
    environment:
      <<: *worker-env
      OPENCODE_MODE: websocket
      WS_ROLE: server
      HEALTH_PORT: ${HEALTH_PORT:-8443}
      OPENCODE_SERVE_PORT: ${OPENCODE_SERVE_PORT:-4096}
      OPENCODE_SERVE_HOST: ${OPENCODE_SERVE_HOST:-127.0.0.1}
      WS_BIND_HOST: ${WS_BIND_HOST:-0.0.0.0}
      WS_PORT: ${WS_PORT:-9090}
      WS_PATH: ${WS_PATH:-/ws/agent}
    ports:
      - "${HEALTH_PORT:-8443}:${HEALTH_PORT:-8443}"
      - "${WS_PORT:-9090}:${WS_PORT:-9090}"
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://127.0.0.1:${HEALTH_PORT:-8443}/health || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 12
      start_period: 30s
    restart: unless-stopped

  agent-smoke:
    <<: *worker-base
    profiles: ["smoke"]
    depends_on:
      lunarwing-worker:
        condition: service_healthy
    entrypoint: ["bun", "run", "/app/scripts/smoke_test.ts"]
    command: []
    environment:
      <<: *worker-env
      SMOKE_WS_HOST: lunarwing-worker
      SMOKE_WS_PORT: ${WS_PORT:-9090}
      SMOKE_HEALTH_PORT: ${HEALTH_PORT:-8443}
    restart: "no"

  opencode-cli:
    <<: *worker-base
    profiles: ["cli"]
    command: ["--mode", "cli", "--", "${TASK_PROMPT:-Write a hello world Python script}"]
    environment:
      <<: *worker-env
      OPENCODE_MODE: cli
    stdin_open: true
    tty: true
    restart: "no"

volumes:
  opencode-data:
  opencode-config-home:
